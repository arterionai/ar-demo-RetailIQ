node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Artefactos generados por `tsc -b` al compilar el proyecto node (tsconfig.node.json)
# a partir de vite.config.ts — no deben commitearse.
/vite.config.js
/vite.config.d.ts

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Captura cruda de referencia visual de palaciodehierro.com (uso interno, no distribuir/commitear
# — ver docs/constitution.md §7.2). Solo assets puntuales extraídos de aquí se copian a src/.
brand-reference/
